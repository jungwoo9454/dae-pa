# 🧅 대파(대용량 파티원) 기획안

> 공고 → 참여 → 채팅 → 정산까지 한 번에 끝내는 동네 공동구매(공구) 플랫폼

| 항목 | 내용 |
| --- | --- |
| 개발 기간 | 2026.08.10 ~ 2026.08.12 (3일) |
| 팀 구성 | 4인 |
| 문서 버전 | v1.0 (2026.08.10) |

---

## 1. 문제 정의

- 혼자 시키면 배달비가 아깝고, 최소 주문 금액을 못 맞춘다.
- 대용량 생필품·식료품은 1인 가구가 사기엔 양이 너무 많다.
- 기존에는 아파트 단톡방/당근 등에서 모집 → **정산이 수기**라서 금액 계산, 입금 확인, 리마인드가 전부 사람 손을 탄다.

**→ 대파는 모집부터 정산까지의 전 과정을 한 서비스 안에서 자동화한다.**

## 2. 타깃 사용자

- 배달비를 나누고 싶은 1인 가구 / 자취생
- 대용량 상품을 나눠 사고 싶은 이웃 (코스트코, 창고형 마트 등)
- 총무 역할이 귀찮아서 공구를 안 여는 잠재 주최자

## 3. 핵심 컨셉

1. **버튼 한 번 참여** — 공고 카드에서 즉시 참여, 채팅방 자동 입장
2. **실시간** — 참여 인원 진행바, 남은 시간, 상태 배지가 실시간 반영
3. **정산 자동화** — 영수증 인식 → 1/N 계산 → 입금 체크리스트 → 리마인드
4. **대파페이** — 지갑 잔액으로 정산 요청 자동 결제, 계좌/토스 링크 송금도 지원

---

## 4. 기능 명세

### 4.1 공구 공고 올리기

- 카테고리 선택: `식료품` `배달음식` `생활용품` `대량구매` `기타`
- 공통 입력: 제목, 설명, 총 금액(예상), 목표 인원, 마감 시간, (선택) 이미지
- **배달음식 전용 폼**: 가게 링크 입력 + 짧은 마감 프리셋(15분/30분/1시간)
- 작성 중 우측(모바일: 하단)에 **실시간 카드 미리보기** 표시
- 등록 시 **공구 채팅방 자동 생성** + 주최자 자동 입장

### 4.2 공구 목록 · 상세 (실시간)

- 목록: 카테고리 필터, 상태 필터, 카드형 리스트
- 카드/상세 공통 실시간 요소
  - 목표 인원 대비 현재 참여자 **진행바**
  - 남은 시간 **카운트다운**
  - 상태 배지: `모집중` `마감임박` `정산중` `마감`
    - `마감임박`은 별도 상태가 아니라 **마감 30분 전부터 파생 표시**
  - **1인당 금액 자동 계산**: `총 금액 ÷ (현재 참여자 수)` 실시간 갱신
- **버튼 한 번 즉시 참여** → 참여 즉시 채팅방 입장 + 시스템 메시지 발생

### 4.3 총 금액 변경

- 정산 시작 전까지 **주최자만** 총 금액 수정 가능
- 변경 시: 참여자 전원에게 알림 + 1인당 금액 즉시 재계산 + 채팅방 시스템 메시지
- **정산 시작 후에는 금액 잠금** (수정 UI 비활성화 + 서버에서 거부)

### 4.4 채팅 커뮤니티

- **동네 라운지**: 전체 공개 자유 수다방 (기본 입장)
- **공구별 채팅방**: 공고 등록 시 자동 생성, 참여자만 입장
- **공구 카드 공유**: 라운지/다른 방에 공구 카드를 말풍선으로 전송 → 카드에서 바로 참여
- **시스템 메시지**: 참여/탈퇴/마감/금액 변경/정산 시작/입금 완료 이벤트 표시
- **채팅방 검색**: 방 이름/공구 제목 기준 검색

### 4.5 정산 시스템

- 주최자가 `정산 시작` → 공구 상태 `정산중` 전환 + 금액 잠금
- **총액 확정**: 주최자가 최종 총액을 수동 입력 + (선택) 영수증 사진 첨부
  - ⏸ **영수증 자동 인식(Claude API 비전)은 보류** — 3일 범위에서 제외, 여유 시 후순위 검토
- 분배 방식
  - 기본: **균등 1/N**
  - 개별 금액 조정 가능 (단, **배달비는 항상 자동 균등 분배**)
  - 계산식: `개인 부담금 = 개인 항목 금액 + (배달비 ÷ 참여자 수)`
- **총액 확정 절차**: 영수증 사진이 있으면 첨부로 증빙, 없으면 **참여자 동의 투표**(과반 동의 시 확정)
- **입금 현황 체크리스트**: 참여자별 입금 여부 표시, 미입금자 **리마인드 알림** 전송
- 전원 입금 완료 → 공구 상태 `마감`

### 4.6 대파페이 지갑

- 잔액 조회 / 충전 / 출금 (실제 PG 없이 모의 충전)
- **정산 요청 자동 결제**: 잔액이 충분하면 원터치 결제 → 입금 체크리스트 자동 반영
- 이용 내역 (충전/출금/결제/수령)
- 잔액 부족 시: 주최자의 **계좌번호 복사 / 토스 링크**로 외부 송금 → 수동 입금 확인

### 4.7 프로필

- **팝오버 방식** (아바타 클릭 시)
- 참여 횟수 / 주최 횟수
- **정산 신뢰도 배지** (기한 내 입금율 기반)
- 내 공구 내역 (참여 중 / 주최 중 / 완료)

### 4.8 설정

- 알림 설정: 마감 임박 알림, 입금 요청 알림 on/off
- 정산 받을 계좌 등록
- 기본 송금 앱 선택 (토스 등)
- 계정 관리 (닉네임 변경, 로그아웃, 탈퇴)

---

## 5. 공구 상태 흐름

```
모집중 (recruiting)
  │  마감 30분 전 → 배지만 "마감임박"으로 표시 (파생 상태)
  ▼  주최자 "정산 시작" 또는 마감 시간 도달
정산중 (settling)   ← 총 금액 잠금
  ▼  전원 입금 완료
마감 (completed)

* 어느 단계에서든 주최자 취소 → 취소 (canceled)
```

## 6. 화면 구성

| 화면 | 주요 요소 |
| --- | --- |
| 홈 (공구 목록) | 카테고리/상태 필터, 실시간 카드 리스트, 공고 작성 버튼 |
| 공구 작성 | 카테고리 선택, 폼(배달음식 전용 분기), 실시간 카드 미리보기 |
| 공구 상세 | 진행바, 카운트다운, 상태 배지, 1인당 금액, 참여 버튼, 채팅방 이동 |
| 채팅 | 방 목록(라운지+공구방), 채팅방(시스템 메시지·카드 말풍선), 검색 |
| 정산 | 영수증 업로드/인식 결과, 분배 편집, 동의 투표, 입금 체크리스트 |
| 지갑 | 잔액, 충전/출금, 이용 내역, 정산 결제 |
| 프로필 | 팝오버(통계·신뢰도 배지), 내 공구 내역 |
| 설정 | 알림, 계좌, 송금 앱, 계정 관리 |

## 7. 데이터 모델 초안 (Supabase Postgres)

```
profiles            id, nickname, avatar_url, bank_account, transfer_app, trust_score
group_buys          id, host_id, title, description, category, store_link,
                    total_amount, delivery_fee, target_count, deadline,
                    status(recruiting|settling|completed|canceled), created_at
participations      id, group_buy_id, user_id, amount_due, is_paid, paid_at, joined_at
chat_rooms          id, type(lounge|group_buy), group_buy_id?, name
messages            id, room_id, user_id?, type(text|system|group_buy_card),
                    content, payload(jsonb), created_at
settlements         id, group_buy_id, method(receipt|vote), receipt_url,
                    recognized_total, status(pending|confirmed), confirmed_at
settlement_votes    id, settlement_id, user_id, agree(boolean)
wallets             user_id, balance
wallet_transactions id, user_id, type(charge|withdraw|pay|receive), amount,
                    settlement_id?, created_at
notifications       id, user_id, type, payload(jsonb), is_read, created_at
```

- 실시간: `group_buys`, `participations`, `messages`는 Supabase Realtime 구독
- 영수증 이미지: Supabase Storage 버킷 `receipts`

## 8. 기술 스택

| 분류 | 기술 | 선정 이유 |
| --- | --- | --- |
| 프레임워크 | **Next.js 15 (App Router) + TypeScript** | FE/BE(API Route) 통합으로 3일 안에 풀스택 완성 |
| 스타일링 | **Tailwind CSS** | 클로드 디자인 시안이 React+Tailwind 기반 → 시안 코드 이식 용이 |
| BaaS | **Supabase** (Postgres · Auth · Realtime · Storage) | 채팅/진행바 실시간 구독, 인증, 파일 저장을 하나로 해결 |
| 서버 상태 | **TanStack Query v5** | 캐싱 + Realtime 갱신 조합 |
| 클라이언트 상태 | **Zustand** | 경량, 보일러플레이트 없음 |
| 영수증 인식 | ⏸ **보류** (추후 Claude API 비전 검토) | 3일 범위 제외 — 총액은 수동 입력 + 동의 투표 |
| 배포 | **Oracle Cloud VM (2 OCPU · 12GB) + Docker** | 팀 보유 서버 활용, Next.js standalone 컨테이너 (docs/DEPLOY.md) |
| 패키지 매니저 | **pnpm** | 빠른 설치 |

## 9. 3일 개발 일정

| Day | 목표 | 주요 작업 |
| --- | --- | --- |
| **Day 1 (08.10)** | 뼈대 + 공구 코어 | 프로젝트 세팅(Next.js·Supabase·배포 파이프라인), DB 스키마, 간단 로그인, 공구 공고 작성(카테고리·전용 폼·미리보기), 목록/상세 + 실시간 진행바·카운트다운, 즉시 참여 |
| **Day 2 (08.11)** | 채팅 + 금액 | 라운지·공구 채팅방, 시스템 메시지, 카드 말풍선 공유, 채팅방 검색, 총 금액 변경(알림+재계산+잠금), 알림 목록, 대파페이 잔액·충전·내역 |
| **Day 3 (08.12)** | 정산 + 마무리 | 총액 확정(수동 입력·영수증 사진 첨부·동의 투표), 1/N·개별 조정·배달비 균등, 입금 체크리스트·리마인드, 대파페이 자동 결제, 프로필 팝오버·설정, 폴리싱·배포·발표 준비 |

> 개발 중 새 작업이 생기면 즉시 이슈로 등록하고 `P0/P1/P2` 라벨로 우선순위를 정한다. Day 3 오후는 반드시 버퍼로 남긴다.

## 10. 범위 제외 (Out of Scope)

- **영수증 자동 인식** (Claude API 비전) — 보류, 여유 시 후순위
- 실제 PG 결제 연동 (대파페이는 모의 충전/결제)
- 위치 기반 동네 인증 (단일 동네 가정)
- 모바일 푸시 알림 (인앱 알림만)
- 관리자 페이지

## 11. 데모 시나리오 (Day 3 리허설용)

1. 주최자가 배달음식 공구 공고 작성 (가게 링크 + 30분 마감) → 카드 미리보기
2. 참여자 2명이 목록에서 버튼 한 번으로 참여 → 진행바·1인당 금액 실시간 변동
3. 라운지에 공구 카드 공유 → 카드에서 세 번째 참여자 합류
4. 주최자가 총 금액 변경 → 전원 알림 + 재계산 확인
5. 정산 시작 → 총액 확정(수동 입력 + 영수증 사진 첨부) → 배달비 균등 1/N
6. 참여자 1명은 대파페이 자동 결제, 1명은 토스 링크 송금 → 체크리스트 갱신
7. 전원 입금 → 공구 `마감` + 신뢰도 배지 반영
